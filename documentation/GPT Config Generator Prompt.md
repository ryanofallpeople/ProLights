```
generate a json dictionary with the key being the name of a color such as "red" or "green" and the value being the RGB color values for that color.

For example "green":[0, 255, 0]
```

```
Add light versions of each color and more common colors such as "gold" and "steel blue"
```